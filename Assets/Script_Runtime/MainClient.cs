using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class MainClient : MonoBehaviour
{
    // Start is called before the first frame update
    void Awake()
    {
        Debug.Log("MainClient Awake");
    }

    // Update is called once per frame
    void Update()
    {

    }
}
